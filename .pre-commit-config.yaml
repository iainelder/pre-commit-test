repos:
  - repo: local
    hooks:
      - id: remark
        name: remark
        entry: npx remark --quiet --frail
        language: node
        types: [markdown]
        additional_dependencies: 
          - "remark-cli@^11.0.0"
          - "remark-preset-lint-consistent@^5.1.2"
